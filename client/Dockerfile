# Placeholder Dockerfile for client (React UI)
FROM node:20-alpine
WORKDIR /app
COPY . .
RUN echo "To implement: install dependencies and build client"
CMD ["sh", "-c", "echo 'Run client service here (e.g. npm start)' && tail -f /dev/null"]
EXPOSE 3000